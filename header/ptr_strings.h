char* read_text(const char* buffer, size_t size, FILE* stream);
int swap_strings(char* text[], size_t s1_ptr, size_t s2_ptr);
int print_strings(char* text[]);
int q_sort_strings(char* text[]);
int free_array(char* text[]);
